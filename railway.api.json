{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm turbo run build --filter=@tumaa/api..."
  },
  "deploy": {
    "startCommand": "pnpm --filter @tumaa/api start",
    "restartPolicyType": "ON_FAILURE"
  }
}
