{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm turbo run build --filter=@tumaa/scraper... && pnpm --filter @tumaa/scraper exec playwright install --with-deps chromium"
  },
  "deploy": {
    "startCommand": "pnpm --filter @tumaa/scraper scheduler",
    "restartPolicyType": "ON_FAILURE"
  }
}
